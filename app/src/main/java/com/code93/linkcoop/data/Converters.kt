package com.code93.juliopaprika_menuvirtual.data

import androidx.room.TypeConverter
import com.google.gson.Gson
import com.google.gson.reflect.TypeToken

class Converters {

/*    @TypeConverter
    fun listToJson(value: List<Direccion>?) = Gson().toJson(value)

    @TypeConverter
    fun jsonToList(value: String) = Gson().fromJson(value, Array<Direccion>::class.java).toList()*/

}